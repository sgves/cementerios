# Miniaturas del selector de mapa base

Imágenes locales usadas solo como vista previa en `MapBasemapToggle`:

- `basemap-orthophoto.jpg` — ejemplo visual de vista aérea / ortofoto.
- `basemap-osm.png` — captura de mapa tipo OpenStreetMap (blog OSM, julio 2025).

No sustituyen las teselas reales del mapa (OSM/WMS); son únicamente thumbnails de UI.
